import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { useEffect } from "react";
import { AuthForm } from "@/components/seconded/forms";
import { supabase } from "@/integrations/supabase/client";
import { hasDemoAdminSession } from "@/lib/demo-admin";
export const Route=createFileRoute("/login")({head:()=>({meta:[{title:"Login — SecondEd"},{name:"description",content:"Login to your SecondEd student marketplace account."},{property:"og:title",content:"Login — SecondEd"},{property:"og:description",content:"Access your SecondEd student marketplace account."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:AuthPage});
function AuthPage(){const navigate=useNavigate();useEffect(()=>{let active=true;const redirectIfSignedIn=async()=>{await new Promise<void>((resolve)=>window.requestAnimationFrame(()=>resolve()));if(!active)return;if(hasDemoAdminSession()){await navigate({to:"/admin",replace:true});return}const{data}=await supabase.auth.getUser();if(active&&data.user)await navigate({to:"/dashboard",replace:true})};void redirectIfSignedIn();return()=>{active=false}},[navigate]);return <div className="page-container grid min-h-[600px] place-items-center py-10 sm:min-h-[680px] sm:py-14"><div className="surface-card w-full max-w-md p-6 sm:p-8"><p className="eyebrow">Welcome back</p><h1 className="mt-2 font-display text-3xl font-extrabold">Login to SecondEd</h1><p className="mb-7 mt-2 text-sm leading-6 text-muted-foreground">Continue buying, selling, and discovering resources.</p><AuthForm/></div></div>}
